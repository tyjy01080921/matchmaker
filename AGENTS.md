# Project Notes

- Build a client-only React app for Cloudflare Pages.
- Keep participant data in browser storage unless a backend is explicitly added.
- Keep match generation logic in `src/matchmaker.ts` and cover risky changes with tests.
- UI copy should be concise Korean operational text.
- Run `npm test`, `npm run lint`, and `npm run build` before handing off meaningful changes.
